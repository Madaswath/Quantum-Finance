import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.fiduciary.quantumwealth',
  appName: 'Quantum Wealth',
  webDir: '../frontend/dist/app/browser'
};

export default config;
